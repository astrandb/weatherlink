[bumpver]
current_version = 2024.1.0b0
version_pattern = YYYY.MM.PATCH[PYTAGNUM]

[bumpver:file_patterns]
setup.cfg =
	current_version = {version}

**/const.py =
	VERSION = "{version}"

**/manifest.json =
	"version": "{version}"
